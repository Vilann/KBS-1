<!DOCTYPE html>
<html>
  <head>
    <title>ZHTC - Login</title>
    <?php
$nu=time();
for ($inlogpoging=0; $inlogpoging>3;$inlogpoging++) {
};
while ($nu<$kwartier) {
    $inlog=true;
}
$ip = getenv("REMOTE_ADDR") ;
$datum= date('Y-m-d');
$kwartier=time() + (0 * 0 * 15 * 00);
print("Hallo de tijd is  $kwartier". date('Y-m-d'));
print("$ip");


//|| empty ($emailadres) || (empty ($bericht))
